"""CNN-xLSTM model, copied verbatim from ``DL-segment-dose-calculation/model``.

These files are byte-for-byte the trained architecture apart from import
rewiring: any edit here risks silently changing ``state_dict`` keys and
breaking checkpoint loading.
"""
